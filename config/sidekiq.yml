:concurrency: 5
:queues:
  - default
  - critical
:scheduler:
  enabled: true
  schedule:
    rca_detector_job:
      every: ["5s"]   # every 5 minutes
      class: "RcaDetectorJob"
    alert_dispatcher_job:
      every: ["2s"]   # every 2 minutes
      class: "AlertDispatcherJob"
